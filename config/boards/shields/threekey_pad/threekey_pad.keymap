#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer: default_layer {
            bindings = <
                &kp LCTRL(&kp C)    &kp LCTRL(&kp V)    &kp LALT(&kp TAB)    &mo 1
            >;
        };
    };
};
